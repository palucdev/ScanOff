<?xml version="1.0" encoding="utf-8"?>
<!--
    Folder card used in the horizontal strip on the Home screen.
    Mockup shows a square-ish dark card with:
      - Coloured folder icon (tinted per-folder at runtime)
      - Folder name
      - File count subtitle
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Widget.Material3.CardView.Filled"
    android:layout_width="120dp"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="?attr/colorSurfaceContainer"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Folder icon inside a tinted circle -->
        <FrameLayout
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginBottom="12dp">

            <View
                android:id="@+id/item_folder_circle"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="@drawable/circle_background" />

            <ImageView
                android:id="@+id/item_folder_icon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_folder_24px"
                android:contentDescription="@string/folder_icon_desc"
                app:tint="?attr/colorPrimary" />

        </FrameLayout>

        <!-- Folder name -->
        <TextView
            android:id="@+id/item_folder_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            android:textColor="?attr/colorOnSurface"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Work" />

        <!-- File count -->
        <TextView
            android:id="@+id/item_folder_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            tools:text="12 files" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>

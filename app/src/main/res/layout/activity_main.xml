<?xml version="1.0" encoding="utf-8"?>
<!--
    Root activity layout.
    Structure:
      CoordinatorLayout
        └─ LinearLayout (vertical, fills screen)
              ├─ NavHostFragment  (weight=1, scrollable content area)
              └─ BottomNavigationView (fixed at bottom)

    The BottomNavigationView sits outside the NavHost so it persists
    across all top-level destinations and is hidden for the full-screen
    scanner destination at runtime via code.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Nav host fragment occupies all available space above the bottom nav -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment_content_main"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            app:defaultNavHost="true"
            app:navGraph="@navigation/nav_graph" />

        <!-- Bottom navigation bar -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_nav_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorSurface"
            app:menu="@menu/bottom_nav_menu"
            app:labelVisibilityMode="labeled"
            app:itemIconTint="@color/bottom_nav_icon_tint"
            app:itemTextColor="@color/bottom_nav_icon_tint" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
